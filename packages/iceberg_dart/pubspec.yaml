name: iceberg_dart
description: A CLI package in the iceberg_dart workspace.
version: 1.0.0
repository: https://github.com/cedricziel/iceberg_dart

resolution: workspace
environment:
  sdk: ^3.10.1

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
