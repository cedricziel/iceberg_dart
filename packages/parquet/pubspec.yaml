name: parquet
description: Schema-first Parquet reading and writing for Dart.
version: 1.0.0
repository: https://github.com/cedricziel/iceberg_dart

resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
